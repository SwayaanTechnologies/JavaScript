const targetElement = document.getElementById('target');
targetElement.addEventListener('mouseover', () => {
  console.log('Mouse is over the target element!');
  // You can perform other actions here, such as sending a request to your Node.js server
});
